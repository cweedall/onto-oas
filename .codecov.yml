codecov:
  require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        target: 10%
        threshold: 1%
        informational: true

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: true
